# 00 — System Audit Board

> **GIAI ĐOẠN 1 ĐÃ HOÀN TẤT.** 14/14 module được audit, mỗi module đủ 8 tài liệu.
> Tổng: 148 luồng nghiệp vụ được chấm điểm và phân loại.

| Trạng thái | Nghĩa | Số luồng |
|---|---|--:|
| `PASS` | Nghiệp vụ đúng, an toàn, dễ dùng — **giữ nguyên** | **31** |
| `PASS_WITH_MINOR_UI_FIX` | Nghiệp vụ đúng; chỉ chỉnh giao diện, **không đổi nghiệp vụ** | **48** |
| `NEEDS_IMPROVEMENT` | Có vấn đề về số bước/trạng thái/liên kết/quy tắc | **47** |
| `CRITICAL` | Rủi ro dữ liệu, phân quyền, hoặc không hoàn thành được nghiệp vụ | **15** |
| `NEEDS_CONFIRMATION` | Source code không đủ để kết luận — cần user chốt | **7** |

**Kết luận tổng: 79/148 luồng (53%) đang tốt và phải giữ nguyên.** Hệ thống **không** cần làm lại.

> 📌 **Cách đọc bảng này khi Giai đoạn 2B đang chạy.** Các con số trên là **kết quả chấm điểm
> của Giai đoạn 1** và không được sửa lại. Khi 2B đóng được một luồng thì ô đó mang **gạch
> ngang + ghi chú ngày và mã quyết định**, chứ điểm gốc vẫn giữ nguyên để còn đối chiếu.
> Trạng thái "đã làm / chưa làm" của 2B là ở
> [`ui-redesign/16_PHASE_2B_IMPLEMENTATION_LOG.md`](ui-redesign/16_PHASE_2B_IMPLEMENTATION_LOG.md).

---

## 1. Tổng quan theo module

| Module | Số luồng | Điểm TB /75 | CRITICAL | Trạng thái module | Tài liệu |
|---|--:|--:|--:|---|---|
| M01 — Auth & Tài khoản | 12 | 43 | ~~4~~ → **0** | ✅ **XONG 2026-07-24** (2B, cả ba đợt A · B · C) — A: F02·F09·F10; B: **F03·F12** (cấp tài khoản tại `/staff/[id]` + đổi vai trò giữ đăng nhập + trần vai trò D-102); C: **F08** (xóa tài khoản GIỮ lịch sử vai trò — FK `set null`, D-101) | [modules/M01-AUTH-ACCOUNT/](modules/M01-AUTH-ACCOUNT/) |
| M02 — Cấu trúc học vụ | 11 | 50 | ~~2~~ → **0** | ✅ **XONG 2026-07-26** (2B, cả ba đợt A · B · C) — A: **F02 đóng** (sinh lớp hết báo thành công giả; danh mục 19 lớp chuẩn đi theo migration; D-112 siết quyền năm học về Super Admin; D-113 cảnh báo trước khi đặt hiện hành) + F01·F03·F04 có phản hồi thật. B: **F07 · F08 đóng** (chi tiết lớp neo vào năm học; màn hình "Cài đặt lớp" đưa `updateClass` vào dùng sau khi nằm không call site từ Phase 1; **D-115/D-116** mốc kết thúc học kỳ 1). C: **F09 đóng** — chốt sổ năm học có bảng kiểm tiền điều kiện + gõ lại mã + lý do (I7/D-73), **hàng rào ghi bằng RLS** cho năm đã đóng (I8/BR-M02-N06, Super Admin ngoại lệ **D-117**, phạm vi ghi danh + lớp **D-118**), **D-70** siết quyền đọc lớp/năm học của phụ huynh–thiếu nhi, **D-119** đóng năm không tự đóng lớp, **D-120** hạn giữ dữ liệu chặn lưu trữ. Ba câu `NEEDS_CONFIRMATION` (Q-M02-04/08/09) **đã được chủ dự án chốt 2026-07-26** | [modules/M02-ACADEMIC-STRUCTURE/](modules/M02-ACADEMIC-STRUCTURE/) |
| M03 — Thiếu nhi & Phụ huynh | 13 | 50 | ~~2~~ → **0** | ✅ **XONG 2026-07-28** (2B, cả ba đợt A · B · C) — A: **F10 đóng** ("Tạm nghỉ" lần đầu chạy được kể từ Phase 2; thêm "Khôi phục"; sáu thao tác ghi im lặng nay nói ra kết quả). B: **F13 đóng** (cảnh báo trùng dùng chung định nghĩa với đường Excel) + **F03** (danh sách ~900 em có tìm không dấu · lọc ngành/lớp/trạng thái · phân trang) + **F02/F09** (tạo hồ sơ và xếp lớp trong một giao dịch) + **D-63/D-123** nới quyền ghi hồ sơ cho Trưởng/Phó ngành và **D-124** nới quyền đọc người giám hộ trong ngành. C: **F06 đóng** (lưu trữ hồ sơ và ghi danh đổi trong MỘT giao dịch — **D-130** hai trục trạng thái đi cùng nhau; **tiêu chí S-11 từ SAI thành đúng**: em đã lưu trữ thì Giáo lý viên mất quyền đọc hồ sơ và sức khoẻ) + **F12 đóng** (31/75 — thấp nhất module: nay sửa được liên lạc phụ huynh và đổi được người giám hộ, có xác nhận nêu đủ ba tên) + **F08 đóng** (sửa/xoá bản ghi bí tích, **D-128**) + **D-127** (Q-M03-02 chốt: vai trò ngành và Giáo lý viên ghi được sức khoẻ/bí tích trong phạm vi) + **D-67/D-129** (mức đọc riêng của Thủ quỹ qua cửa sổ hẹp, kèm dấu "hoàn cảnh khó khăn"). Ba câu `NEEDS_CONFIRMATION` (Q-M03-02/05/06) **đã được chủ dự án chốt** | [modules/M03-STUDENTS-GUARDIANS/](modules/M03-STUDENTS-GUARDIANS/) |
| M04 — Nhân sự | 9 | 44 | ~~5~~ → **0** | ✅ **XONG 2026-07-25** (2B, cả ba đợt A · B · C) — A: F05·F06 (chuyển lớp một bước, D-105); B: F01·F02·F04·F07·F08 (danh sách dùng được, chống trùng, xóa hồ sơ chưa từng dùng); C: **F09** (đội ngũ lớp mở thẳng hồ sơ GLV) + thu hẹp `/admin` về tra cứu/ngoại lệ (D-111) | [modules/M04-STAFF/](modules/M04-STAFF/) |
| M05 — Điểm danh | 16 | 60 | 0 | ✅ **XONG 2026-08-03** (2B, cả ba đợt A · B · C) — **A: toàn bộ nhóm "đúng đắn dữ liệu".** TB-01 (sáng Chúa nhật form đổ sẵn buổi thứ Năm **tuần trước** — máy chủ chạy UTC, 06:00 giờ Việt Nam là 23:00 thứ Bảy UTC; E2E cũ không bắt được vì nó tự sinh ngày rồi `fill` tay) · TB-02 (`deriveSessionState()` là **một chỗ duy nhất**; trước đó buổi đã khóa hiện *"Đã chốt"* ở danh sách và *"Đã khóa"* khi mở ra) · TB-04 (ba mã lỗi phiên chỉnh sửa thay một điều kiện gộp gọi tên một người không tồn tại) · TB-07 (một em rời lớp khóa cứng **cả buổi đã diễn ra**) · **D-140/nợ #19** (em "Tạm nghỉ" ra khỏi danh sách điểm danh; cái bẫy nằm ở `roster_size` khi chốt — một truy vấn **chép tay** cùng luật) · **D-139** hai Cha xem được (chỉ đọc) · **nợ #18** hàng rào năm học đã đóng, đặt **trong bốn RPC** vì `security definer` bỏ qua RLS · nợ #20 · nợ #14. pgTAP `041` **31 bài JWT thật**.<br>**B: hoàn thiện nghiệp vụ đơn xin nghỉ + D-75.** TB-06 (`acknowledgeAbsenceRequest` viết từ Phase 3 mà **không màn hình nào gọi** — trạng thái `acknowledged` chưa bao giờ đạt tới được, mọi đơn nằm ở "Đang chờ" vĩnh viễn) · **D-75 siết bằng QUYỀN CỘT**, không bằng nhánh policy: RLS lọc theo **dòng** không theo **cột**, mà phụ huynh **đang** đọc đúng dòng của con mình và phải tiếp tục đọc được · TB-11/**D-141** (chặn theo **trạng thái buổi**, không theo ngày). pgTAP `042` **35 bài JWT thật**.<br>**C xong 2026-08-03 ⇒ MODULE ĐÓNG. Trạng thái, an toàn thao tác và điện thoại — 0 migration, 0 thay đổi phân quyền.** **TB-03** hộp xác nhận trước khi chốt (bảng phân bố **hai cột** tính từ bản nháp, kèm **tên riêng** em có đơn mà vẫn để "Có mặt"; bấm Huỷ ⇒ **không request nào** đi tới máy chủ) · **TB-05** đồng hồ phiên chỉnh sửa lấy từ giá trị máy chủ, và bị tiếp quản thì trang **không** lặng lẽ chuyển chỉ-đọc nữa — có băng-rôn `role="alert"` và ô chép lại phần chưa lưu, **không** tự gửi gì lên · **TB-09** badge cảnh báo chuyên cần đọc `v_student_attendance_summary` (`security_invoker`, lọc theo `(năm học, lớp)` để khỏi nối thêm một vòng gọi) · **U-10/D-142** hàng nút **ba lựa chọn** + "…" thay 5 ô chọn thả xuống · **U-21/D-143** hàng em gấp lại: **~9.000px → ~1.800px** cho lớp 50 em · U-11 lọc + tìm không dấu, thuần client, đọc **bản nháp đang gõ** · U-17/U-18 thông báo chuyển **vào thanh hành động** cạnh nút vừa bấm · U-24/U-25 `aria-live` + trả focus sau lượt ghi. 🔴 **Bắt được một lỗi thật của M05-B khi chạy E2E:** `AbsenceReviewPanel` trả về trạng thái rỗng **trước khi** dựng dòng thông báo, nên ghi nhận đơn **cuối cùng** thì câu *"Đã ghi nhận đơn của {tên}"* bị chính lượt làm mới nó kích hoạt xoá mất — vi phạm D-61 ở đúng ca thường gặp nhất, và trước đó bị ghi nhầm là nợ #10 | [modules/M05-ATTENDANCE/](modules/M05-ATTENDANCE/) |
| M06 — Giáo án | 11 | 65 | 0 | ✅ **XONG 2026-08-05** (2B, cả ba đợt A · B · C) — **A: nhóm "S, độc lập" và "S, siết an toàn".** Thông báo THÀNH CÔNG hiện màu đỏ kèm `role="alert"` (`FormMessage` thiếu `tone`) · ô chọn người dạy mời chọn người mà DB chắc chắn từ chối, và cái bẫy khi sửa **nặng hơn lỗi**: `<select>` mất giá trị đang giữ sẽ **âm thầm** đổi người dạy khi chỉ đổi mỗi ngày · câu lỗi viết kỹ từ Phase 4 **chưa từng hiện ra một lần nào** · trần tệp 5 MB là lời hứa **không thể giữ** (`bodySizeLimit` 4,5 MB của nền tảng) ⇒ về 4 MB đúng D-137 · phụ huynh gõ thẳng URL quản trị nhận 404 · kèm **nợ #14** và **nợ #20**. **B: đợt duy nhất có migration** — **D-144** 🔴 SIẾT (ba vai trò cấp xứ đoàn chỉ còn XEM giáo án; giải mâu thuẫn giữa `docs/05` và WF-07, Super Admin giữ quyền ghi làm đường thoát khi lớp chưa có đại diện) · **D-145** NỚI (hệ thống mang **hai định nghĩa "thuộc lớp"**, phần đọc dùng cái hẹp còn phần ghi dùng cái rộng ⇒ có người ghi được mà tải lại trang là **trắng**; tệp đính kèm đi theo) · **nợ #18** hàng rào năm học đã đóng vào cả sáu policy ghi · **D-146** chống ghi đè bằng kiểm phiên bản, với cái bẫy micro giây ↔ mili giây. pgTAP 1033 → **1061**. **C: giao diện thuần, 0 migration** — biểu mẫu 12 trường vào **hộp thoại dạng drawer** (**D-148**, một đường đi cho cả ba viewport) · ba nhóm trường, hai nhóm gập sẵn **có bộ đếm ô đã điền** (**D-149**) · hai `window.confirm` cuối của module thành `ConfirmDialog` nêu cả điều câu cũ giấu mất (**xoá mục là xoá luôn tệp đính kèm**) · vá một lỗ **D-61** (sửa mục xong không có một chữ xác nhận nào) | [modules/M06-TEACHING-PLANS/](modules/M06-TEACHING-PLANS/) |
| M07 — Kiểm tra & Bảng điểm | 18 | 58 | 0 | ✅ **XONG** (2B, **ĐÓNG 2026-08-06** — A · B · C) — **A: nhóm rủi ro rất thấp của `07` §7, 0 migration.** 🔴 **TB-M07-08/S-10** — kiểm soát chống công thức lạ trong file Excel đang áp dụng **nửa vời**: tên thánh/họ tên qua bộ lọc, **tiêu đề cột điểm thì không**, mà đó là ô văn bản tự do 120 ký tự do Giáo lý viên đặt. Và **bài kiểm cũ là một phần của lỗi** — nó gọi hàm lọc **tách rời** rồi đếm số phần tử header, nên xanh vĩnh viễn bất kể hàm dựng bảng có gọi bộ lọc hay không; bài mới **quét mọi ô văn bản**. 🔴 **9b (`07` §7 gọi là điều kiện tiên quyết)** — "Lưu điểm" ghi **cả roster kể cả ô trống**, gốc rễ của **ba** lỗi: cột tạo nhầm **không bao giờ xóa được nữa** (F04) · người lưu sau **ghi đè sạch** điểm người trước, không cảnh báo (F06) · một cú bấm đóng dấu *"đang chỉnh tay"* lên **cả 50 em** làm đề xuất chuyên cần tự động chết (F07). Nay chỉ gửi **ô đã đổi**. **D-150** (TB-M07-07) — cổng phụ huynh và bảng điểm nội bộ nói hai con số trung bình khác nhau, không chỗ nào giải thích; chốt **cả hai con số lấy từ phần đã công bố**, vì AC-07-01 (*"3/5 cột"*) mâu thuẫn với AC-02-03 (*"không thấy dấu vết cột tồn tại"*) trong cùng một tài liệu. **TB-M07-09** hệ số mặc định đọc từ `assessment_type_settings` (bảng có thật từ Phase 5 mà biểu mẫu chưa từng đọc). Kèm câu lỗi tiếng Việt cho `ZodError` ở **6 luồng** · **SW-04** (khóa bảng điểm rồi bấm Lưu vẫn báo *"Đã cập nhật"* — `using` của policy lọc dòng **trước khi trigger chạy**) · **nợ #14** · **nợ #20 ĐÓNG HẲN** · phần "chờ cứng" của **nợ #10**. Đo được: unit **1185/10 skip** · pgTAP **1061/1061** (+0) · **toàn bộ E2E 414/420**, `results.spec.ts` xanh cả ba viewport. **B xong 2026-08-05 — đợt DUY NHẤT có migration của module** (`20260805000200`): 🔴 **D-74 + D-151** — *"ai khóa được bảng điểm"* có **ba tầng nói ba điều khác nhau**, đúng thứ `08_ACCEPTANCE_CRITERIA` §5 gọi là *"mâu thuẫn chưa giải quyết"* và từ chối viết tiêu chí cho tới khi chủ dự án chốt; tệ nhất là phép tính hiện nút **không kiểm lớp**, nên đại diện lớp A thấy nút "Khóa" trên lớp B. Nay cả ba trỏ về `app.can_lock_gradebook`; kèm **AC-10-02** (khóa lần hai không đẩy lùi mốc khóa). ⚠️ **Giảm quyền của người đang dùng — phải báo trước cho Ban điều hành xứ đoàn.** 🔴 **TB-M07-01** — xóa một cột tạo nhầm là việc **không làm được** và câu lỗi *"Cột đã có điểm"* **nói sai**; nay cột chưa có điểm → xóa hẳn (dọn luôn dòng rỗng của lỗi cũ), cột đã có điểm → **ẩn**, và ẩn có hiệu lực **ở tầng cơ sở dữ liệu** chứ không chỉ ở truy vấn ứng dụng (AC-01-03, đo bằng JWT phụ huynh). 🔴 **BR-M07-31** — cờ *"chỉnh tay"* đóng dấu **vô điều kiện** cho cả cột chuyên cần; nay chỉ bật khi khác đề xuất và **tự gỡ** khi trùng lại, kèm **D-153** dọn dấu đặt sai trong dữ liệu hiện có (**thay đổi dữ liệu duy nhất của cả module**). **TB-M07-04** — con số *"đã cập nhật N đề xuất"* đếm gộp cả dòng bị bỏ qua nên **nói sai**; nay trả hai số. 🔴 **TB-M07-05/BR-M07-32** — mức hiển thị nhận xét mặc định là **công khai**, viết vội một câu là ra thẳng cổng phụ huynh; nay mặc định nội bộ. 🔴 **D-152** — bất kỳ ai dạy lớp đều xóa được nhận xét của người khác, mà bảng **không có lịch sử**; nay chỉ tác giả · đại diện lớp · cấp xứ đoàn, và luật đặt vào **cả sửa lẫn xóa**. **Nợ #18** — bốn bảng của module, và module này chứa **cả hai ca**: ba bảng chặn bằng policy, `assessment_scores` chỉ chặn được **trong RPC** vì definer bỏ qua RLS. Đo được: pgTAP **1115/1115** (+54, file `044` toàn bộ JWT thật) · unit **1210/10 skip** (+25). **C xong 2026-08-06 ⇒ ĐÓNG MODULE** — **migration thứ hai của module** (⚠️ đính chính: đợt B tự ghi là *"đợt duy nhất có migration"*, câu ấy sai; câu đúng là *"migration duy nhất của 2B **có đụng dữ liệu**"*). **0 thay đổi phân quyền · 0 dòng dữ liệu bị đụng.** 🔴 **D-154/TB-M07-02** — *"khóa bảng điểm"* và *"công bố kết quả cho phụ huynh"* là **hai việc bị buộc chung làm một**: muốn công bố thêm một cột sau khi khóa thì phải nhờ Quản trị viên hệ thống **mở khóa cả bảng điểm**, tức mở luôn quyền sửa điểm và hệ số của cả lớp, đúng lúc điểm vừa chốt. `07` §1 chấm đây là hạng mục **rủi ro cao nhất module**. Nay tách ra, **cả hai chiều**; **ai được công bố thì không đổi** nên đợt này 0 thay đổi phân quyền. 🔴 **Quả mìn nằm ở bảng KHÁC** — đường công bố qua **ba** lớp kiểm, lớp thứ ba là trigger dòng của `assessment_scores` (trigger đồng bộ cờ chạy một lệnh UPDATE lên bảng ấy, và nó **cũng** ném `GRADEBOOK_LOCKED`); nới hai lớp đầu mà quên lớp thứ ba thì thao tác vẫn hỏng với đúng mã lỗi cũ. **Policy giữ nguyên** ⇒ AC-02-02 vẫn đúng với lệnh gửi thẳng vào cơ sở dữ liệu, đo bằng **6** khẳng định riêng. 🔴 **D-155/TB-M07-06** — ẩn một bảng Top 5 rồi công bố lại là **âm thầm tính lại**: em hạng 5 hôm trước biến mất, không ai được báo, bản cũ không còn ở đâu (F16). Tài liệu khuyến nghị cấm tính lại; **chủ dự án chọn hướng ngược lại** — cho tính lại nhưng bản đang có **xuống lịch sử** (`leaderboard_snapshots`, append-only, **không nhánh đọc cho cổng phụ huynh**). Bảng lịch sử đứng **riêng** chứ không thêm cột vào `leaderboard_entries`, vì bảng ấy là **đúng cái cổng phụ huynh đọc** và `07` §4 cấm đụng vào. Kèm **BR-M07-35** siết đường xóa: phép thử cũ `not is_published` sai **đúng hình dạng F04** — ẩn xong là policy cho qua rồi để khoá ngoại trả lời hộ. 🔴 **Nợ #1 ĐÓNG HẲN** — 4 `window.confirm` **cuối cùng của toàn hệ thống**, `grep` nay ra **0**. **Nợ #21 ĐÓNG** — cột đã ẩn có đường hiện lại (món nợ do chính đợt B mở ra ba ngày trước). Đo được: pgTAP **1158/1158** (+43, file mới `045` đo **cả hai chiều** của ngoại lệ khóa) · unit **1224/10 skip** (+14) · lint 0 warning · typecheck ✓ · build 28/28 · **E2E toàn bộ 410/420** (10 đỏ = **2,4 %**, ⚠️ **tăng** so với 1,4 % của A và B — 4 bài rớt ở `page.waitForURL` là chữ ký **nợ #15**, 6 bài rớt ở lượt làm mới chưa về là chữ ký **nợ #10**; **không bài nào rớt ở một khẳng định nghiệp vụ**, và `results.spec.ts` **xanh trọn vẹn trên `laptop-1366`** gồm cả bốn bước mới. Không kết luận được rằng đợt C vô can: nó cộng thêm ~6 lượt làm mới vào chính spec ấy). **Còn để ngỏ có chủ ý:** hạng mục 9a (khoá lạc quan đầy đủ) — `07` §7 luật 3 dặn *"chỉ làm nếu 9b chưa đủ, đo bằng thực tế sử dụng"*; và **nợ #22** màn hình đọc lại lịch sử Top 5 (dữ liệu + policy + test đã có, chưa có bề mặt) | [modules/M07-ASSESSMENTS/](modules/M07-ASSESSMENTS/) |
| M08 — Chuyển lớp | 10 | 51 | ~~1~~ → **0** | ✅ **XONG 2026-08-08** (2B, cả ba đợt A · B · C) — **A: gói tối thiểu của `07` §3.** Lỗi `CRITICAL` duy nhất của module là **một phép nhân**: `getPromotionsPageData` hỏi hai truy vấn cho **mỗi** ghi danh và đọc **mọi** năm học ⇒ ~1.800 lượt đi về cơ sở dữ liệu cho một lượt mở trang với ~900 em; nay **3–5 lượt gọi cố định**. Kèm **bảng tiến độ theo lớp** lần đầu tồn tại (mỗi con số là một liên kết, đếm trên **cả phạm vi** chứ không trên trang đang xem) · **BR-M08-14** chỉ năm hiện hành · **BR-M08-X2** mặc định giữ nhánh A/B (tài liệu đòi từ `docs/03` WF-11, hệ thống **chưa bao giờ** làm) · 11 tên luật của RPC có câu tiếng Việt riêng thay cho hai câu gộp · **nợ #14**. **0 migration.** **B: đợt duy nhất có migration.** **D-156/D-161** cảnh báo bí tích lớp cuối ngành — cột cờ đã seed từ Phase 2 mà **chưa ai đọc lần nào**; cảnh báo **không** chặn, chỉ buộc nêu ý kiến · **D-157** bảng nhật ký chỉ-ghi-thêm (gửi lại một đề xuất bị từ chối đang **xoá sạch** ai từ chối, lúc nào, vì sao) · **D-158/D-162** bịt đường vòng đóng ghi danh ở **cả hai tầng** · **nợ #18/D-160** hỏi **cả hai** năm học · **D-159** một nút "Chuyển lớp" cho cấp xứ đoàn, **0 thay đổi phân quyền**. 🔴 Quả mìn của đợt là **thứ tự lệnh**, không phải một điều kiện bị quên. **C: ba nợ cuối, 1 migration là một CỬA SỔ HẸP.** **AC-14** nút "Duyệt" lần đầu hỏi lại (nợ mang qua hai đợt vì D-159 sẽ đổi nội dung câu hỏi) · **AC-15** từ chối bắt buộc nêu lý do, chặn ở **cả** màn hình lẫn Zod máy chủ · **AC-20** đề xuất hàng loạt, "Chọn tất cả" lấy **mọi em khớp bộ lọc kể cả trang sau** (chủ dự án chốt 2026-08-08) · **hạng mục 8** hiện tên người đề xuất/người duyệt — và phụ thuộc mà M08-B để ngỏ đã được **đo**: `profiles` **không** cho Trưởng ngành và GLV đại diện đọc tên ai cả, nên mở `list_promotion_actor_names` thay vì nới policy | [modules/M08-PROMOTIONS/](modules/M08-PROMOTIONS/) |
| M09 — Ban & Thiết bị | 19 | 65 | 1 | `PASS_WITH_MINOR_UI_FIX` | [modules/M09-COMMITTEES-EQUIPMENT/](modules/M09-COMMITTEES-EQUIPMENT/) |
| M10 — Thông báo | 9 | 62 | ~~2~~ → **0** | ✅ **XONG 2026-08-10** (2B, cả ba đợt A · B · C trong một phiên) — 🔴 **Cả hai lỗi `CRITICAL` là MỘT lỗi lặp hai chỗ: một dòng `.eq("profile_id", …)` VẮNG MẶT.** Với 8/14 vai trò lỗi này **không tồn tại** — hàng rào cơ sở dữ liệu vốn đã lọc đúng cho họ; với **6 vai trò cấp xứ đoàn** thì chuông đếm chưa đọc của **cả xứ đoàn** (*"99+"* và **không bao giờ về 0**) còn hộp thư **cá nhân** là *"50 dòng người-nhận mới nhất của toàn hệ thống"*, **kể cả nội dung thư riêng gửi cho người khác**. Gốc rễ (**BR-M10-20**): **hàng rào bảo mật bị dùng thay cho bộ lọc nghiệp vụ** — nó trả lời *"được phép thấy gì"*, truy vấn phải trả lời *"muốn thấy gì"*. Sửa ở **truy vấn**, **không** sửa policy (`07` §4). **A: 0 migration, 0 thay đổi phân quyền** — hạng mục 1 (`07` §1 gọi là *"chênh lệch chi phí/giá trị lớn nhất trong cả dự án"*) + AC-02-01/02 (số người nhận thật, cảnh báo khi gửi vào hư không — **không cần migration**, cột `recipient_count` đã có từ Phase 6 mà chưa ai đọc) + **nợ #14** (module mắc **cả hai** vế của D-96, ở cả ba thao tác). 🔴 **Bài quét mã nguồn viết cho đợt này tìm ra CHỖ THỨ BA, ngoài phạm vi audit:** ô *"Thông báo mới nhất"* của `/dashboard` — trang **ai cũng đổ vào ngay sau khi đăng nhập** — mắc đúng lỗi ấy ở **hai** chỗ; thuộc **M11** nhưng đã sửa tại chỗ và bàn giao. **B: 1 migration, 0 thay đổi phân quyền, 0 policy bị sửa** — **D-167** nhánh gửi đích danh, sửa ở **cấu trúc** truy vấn vì phép nối bắt buộc nằm ở `from` nên nó loại người ta ra **trước khi** `case` chạy · **D-165** mã chống gửi đúp (đường tắt idempotent phải dừng **trước** bước chốt người nhận, nếu không `recipient_count` bị ghi đè thành 0) · `app.notification_audience` làm định nghĩa **dùng chung** cho đếm-trước và chốt-người-nhận (**BR-M10-24**) · **AC-06-01** hộp xem lại · **TB-M10-03** bộ chọn "Một người" — chức năng đã đủ ở cơ sở dữ liệu từ Phase 6 mà **chưa từng có một nút bấm nào**. **C: 1 migration, 1 policy bị sửa** — **D-166** thu hồi mềm (tác giả + cấp xứ đoàn, **không giới hạn thời gian**, **lý do bắt buộc**: *"biện pháp an toàn không phải đồng hồ mà là nhật ký"*) · **D-168** hoà giải **hai tài liệu module nói ngược nhau** bằng một **cờ nhân bản** giữ bằng trigger · mục "Tôi đã gửi" · lọc chưa đọc · phân trang thay `limit 50` cứng. 🔴 **Và BR-M10-20 lặp lại TRONG CHÍNH LƯỢT SỬA NÓ** ở bảng thứ hai — bài quét của đợt A không bắt được vì nó chỉ canh **một** bảng; bài học: **một bài kiểm chỉ bảo vệ đúng phạm vi nó quét**. **2 migration · 0 thay đổi phân quyền · 0 dòng dữ liệu bị đụng · 0 quyền ghi mới.** pgTAP **1286/1286** (+53) · unit **1448/16 skip** · E2E **494/507**, `notifications.spec.ts` **21/21 xanh cả ba viewport** | [modules/M10-NOTIFICATIONS/](modules/M10-NOTIFICATIONS/) |
| M11 — Báo cáo & Dashboard | 15 | 57 | 0 | ✅ **XONG 2026-08-12** (2B, ba đợt A · B · C) — module **không có luồng `CRITICAL`**, và biên bản nói rõ vì sao: *"toàn bộ yêu cầu bảo mật của WF-15 đều ĐẠT; các điểm trừ nằm ở nhất quán số liệu, phân biệt rỗng vs bị chặn và trải nghiệm"*. Tức module này không hỏng ở chỗ **cho phép sai**, nó hỏng ở chỗ **nói sai** — và cả ba đợt đều là những lượt chữa một câu nói sai. **A: 1 migration · 1 thay đổi phân quyền (`11` §6 D-66, SIẾT).** 🔴 **D-66 siết bằng cách TÁCH MỘT HÀM LÀM HAI:** `app.can_create_report` từ Phase 6 phục vụ **cả** policy đọc lẫn policy ghi của `report_snapshots`, nên sửa thẳng nó là **lấy luôn quyền xem/tải** của Cha sở/Cha phó — trái đúng câu chữ D-66; mất quyền chốt **đúng ba cái tên**, đo bằng cách liệt kê **14 vai trò × 3 phạm vi**. Cái bẫy đi kèm: **không** được viết hàm hẹp bằng `can_access_sector`/`can_access_class` vì hai hàm ấy **tự gọi `can_global_read()` bên trong** ⇒ siết một nhánh trong ba, và bộ kiểm chỉ đo nhánh `global` sẽ **xanh giả**. **D-169 (F02)** ô "Lớp" đếm đúng phạm vi, sửa **trong view**, không đụng policy `classes`. **TB-04/TB-05** ba lý do bảng trống ra ba câu khác nhau · tham số URL hỏng **thu hẹp và nói ra** · phạm vi mặc định suy từ vai trò · nút "Chốt" **hỏi luật** thay vì chép lại nó. **Nợ #18 ĐÓNG HẲN** (bảng cuối cùng). **B: 1 migration · 1 thay đổi phân quyền (`11` §6 D-67, NỚI — nửa còn nợ).** 🔴 **Hiện trạng đã ĐO chứ không đoán:** Thủ quỹ thấy `0 thiếu nhi · 0 giáo lý viên · 0 lớp` trên trang tổng quan và **0 dòng** ở cả hai bảng báo cáo — *"bị cấm toàn bộ ở cơ sở dữ liệu nhưng được mời vào ở giao diện"*. **D-170 + D-173:** nới bằng **cửa sổ hẹp** phủ ba chỗ (bảng báo cáo · kho bản chốt · bốn ô số tổng quan), **không** thêm `treasurer` vào `can_global_read()` và **không** chuyển hai RPC báo cáo sang `security definer` (thứ `07` §2.1 tự xếp là điểm nguy hiểm nhất của cả module). Hai hàm `_for_treasurer` **gọi thẳng** hai RPC gốc chứ không chép SQL — chép ra bản thứ hai là dựng sẵn hai con số sẽ lệch nhau, tức phá D-52 ngay chỗ nó sinh ra để bảo vệ. **Cố ý KHÔNG sửa `v_dashboard_summary`**: ba trong bốn con số của view lọc bằng RLS, riêng `class_count` lọc bằng vị từ viết tay ⇒ nới trong view là dựng lại **đúng cái bệnh D-169 vừa chữa hôm trước**. **D-171** báo cáo Kết quả ép kỳ = năm học (nhãn từng nói sai nội dung và bản chốt ghi lại cái nhãn ấy **vĩnh viễn**). **D-172 + D-174 + AC-B09** hộp xác nhận nêu bản trùng kèm **tên** người chốt — và chữ "ai" là **bản sinh đôi của cái bẫy `profiles` mà M08-C đã vấp** (Trưởng ngành và GLV đại diện không đọc được tên người khác ⇒ nhúng thẳng là một ô `null` trong im lặng). **C: 1 migration · 0 thay đổi phân quyền.** **TB-03** ngõ cụt của phụ huynh — bản cũ bọc ba thẻ bằng `isStaff` và **bỏ sót thẻ "Cần quan tâm"**, nên bấm tên con là sang `/access-denied` và hết đường; nay đích đến suy từ `audience` ở **một chỗ duy nhất**. **TB-06** kho 5 năm lần đầu có cửa (lọc · phân trang · trang xem lại có băng-rôn bất biến và checksum), kéo theo cái bẫy `profiles` **lần thứ hai** ⇒ `list_report_snapshot_actors`. Redesign `/reports` theo `09` (`Select` · `DataTable` · ba trạng thái rỗng chuẩn) + **AC-B15** vùng `aria-live` · **N-2** · **F05** · **F12**. 🔴 **N-6 — dựng bộ E2E đầu tiên cho `/reports` và `/dashboard`, và nó lộ ngay một khoảng trống của bộ dữ liệu mẫu:** `seed:dev` **không tạo một buổi điểm danh đã chốt nào**, nên nút "Chốt báo cáo" luôn `disabled` và đường ghi nguy hiểm nhất của module sẽ **không bao giờ được chạy tới**; spec tự dựng dữ liệu qua service role, mỗi viewport một tháng riêng để phép đo "bản trùng" không phụ thuộc thứ tự chạy. **3 migration · 2 thay đổi phân quyền (1 SIẾT, 1 NỚI) · 0 `alter table` · 0 backfill · 0 dòng dữ liệu bị đụng.** pgTAP **1385/1385** trên **52 file** (+61 so với trước module) · unit **1531 pass / 16 skip** (+62) · lint **0** · typecheck ✓ · build ✓ **29/29 trang** · `reports.spec.ts` **18/18 xanh** chạy riêng. ⚠️ **Số E2E toàn bộ KHÔNG dùng làm số nghiệm thu:** Docker Desktop hỏng hai lần giữa phiên (engine trả `500` cho mọi lệnh; một truy vấn Supabase đo được **445 giây**), nên lượt chạy đầy đủ mất **3,6 giờ** thay vì ~25 phút và **bị cắt ngang ở 107 xanh / 46 không được chạy** ⇒ không có mẫu số để so với 494/507 của M10-C. **Phải chạy lại khi môi trường ổn** | [modules/M11-REPORTS-DASHBOARD/](modules/M11-REPORTS-DASHBOARD/) |
| M12 — Nhập Excel | 10 | 44 | ~~3~~ → **0** | ✅ **XONG 2026-08-03** (2B, cả ba đợt A · B · C) — **A xong 2026-07-29: gỡ cả ba lỗi CRITICAL trong một đợt** (đúng "gói tối thiểu" của `07_IMPLEMENTATION_IMPACT` §3). **F02/F07** hết câm: năm biểu mẫu vứt giá trị trả về nay dùng `useActionState`, tải lên hỏng đọc được lý do, tải lên xong vào thẳng trang lần nhập, kết quả ghi liệt kê **từng dòng lỗi**. **F08**: xoá lần nhập đã ghi bị chặn **ở cơ sở dữ liệu** (`20260729000100`, pgTAP `039` JWT thật); huỷ = **đánh dấu** (D-131); "Xoá dữ liệu thô" là thao tác riêng giữ nguyên mapping (D-132). **F02** hết mặc định nguy hiểm: dòng trùng chắc chắn mặc định **Ghép** và **chặn ghi** tới khi người duyệt xác nhận (D-133), dò trùng xét cả hồ sơ đã rút. Kèm **SEC-16** từ "hiện đang vi phạm" thành đúng (hết in `sqlerrm` ra màn hình) và **Zod ở biên**. Đo được: unit **947/10 skip** · pgTAP **947/947** · E2E đợt **15/15** · **toàn bộ E2E 351/357**.<br>**B xong 2026-07-29 — module dùng được ở quy mô thật.** **F05** (chọn giới tính từng dòng, 42/75): sổ SYLL thiếu giới tính ở **83% dòng** mà mỗi dòng là **một lần gửi + một lượt dựng lại cả trang** — nay cả trang đi lên trong **một** lượt, kèm "Áp dụng Nam/Nữ cho các dòng đang chọn" (TO-BE 4 / AC-21), và **không** có nút đoán theo tên đệm (BR-M12-36). **F04/F03** (chi tiết batch 50/75, danh sách batch 58/75): trang chi tiết hết đổ toàn bộ 300–900 dòng vào một lượt dựng — **50 dòng/trang + 6 bộ lọc trạng thái**; danh sách lần nhập hết cắt cứng 20 cái, có phân trang, lọc theo năm học (mặc định năm hiện hành, **D-135**) và trạng thái, và **nói ai tải lên**. **D-133 vẫn đứng nguyên**: nút lưu chung cố ý **không** xác nhận hộ dòng trùng chắc chắn, dòng ấy có nút riêng nằm trong khối đối chiếu phải mở ra mới bấm được (**D-136**). **0 migration · 0 thay đổi phân quyền.**<br>**C xong 2026-08-03 ⇒ MODULE ĐÓNG.** **Tải file lỗi/kết quả lần đầu tồn tại** (TO-BE 5): `docs/09` §9 đòi từ Phase 2 mà chưa bao giờ có — hai sheet `LOI` + `KET_QUA`, là **cầu nối duy nhất** tới Giáo lý viên lớp mà **không nới quyền cho ai** (SEC-04b trả **403**); mọi ô chuỗi chống chèn công thức (AC-23/SEC-13) và lỗi ghi **dịch trước khi vào file** để SEC-16 không mở lại ở cửa thứ hai. **Ghi danh bị bỏ qua hết im lặng** (TO-BE 6/AC-24): `on conflict do nothing` từng làm dòng báo `committed` dù em vẫn ở lớp cũ — nay `out_enrollment_created` + cảnh báo nêu **đúng tên lớp**, đếm **riêng** trong kết quả ghi (migration `20260803000100` drop+create, **cấp lại grant**). **Hai cái trần** (**D-137** 4 MB · **D-138** 1.000 dòng) đóng **SEC-10** và **SEC-12** — hai mục `NEEDS_CONFIRMATION` NC-01/NC-02 treo từ Giai đoạn 1. Đo được: unit **998/10 skip** · pgTAP **967/967** · bộ `imports` E2E chạy riêng **42/42** trên ba viewport · **toàn bộ E2E 371/384** (13 đỏ, **0 bài thuộc bốn luồng mới của đợt C**) | [modules/M12-IMPORTS/](modules/M12-IMPORTS/) |
| M13 — Cổng PH & Thiếu nhi | 14 | 50 | ~~**3**~~ → **0** | ✅ **ĐÓNG 2026-08-12 (A · B · C) ⇒ đủ 14/14 module Giai đoạn 2B.** M13-A đóng lối vào/quyền: D-64 một con đi thẳng, nhiều con có danh sách; `/student/*` mặc định đóng; tách `getAccessibleStudents` / `getMyChildren` / `getSelfStudent`; D-25 cho GLV-phụ huynh; D-70/D-75 giữ chặn URL chéo và ghi chú nội bộ. M13-B phân biệt `not_linked` · `no_children` · `no_enrollment` · `no_data`, gắn mật độ comfortable cho hai portal, hoàn thiện cảnh báo/bảng/focus ở 360px. M13-C rà route và kiểm chéo Kết quả · Giáo án · Thông báo; điểm trung bình portal đổi đúng khi ẩn cột. **0 migration · 0 đổi RLS · 0 đổi dữ liệu · 0 đổi quyền.** pgTAP **1385/1385** · unit **1545 pass / 16 skip** · portal E2E **24/24** ở 360/768/1366 · lint 0 · typecheck ✓ · build **29/29**. E2E toàn hệ thống chạy đủ **561/585**; 24 bài đỏ là nợ ổn định/fixture cũ đã phân loại, nên 2B đóng về phạm vi triển khai nhưng chưa tuyên bố sẵn sàng phát hành | [modules/M13-PORTAL/](modules/M13-PORTAL/) |
| M14 — Vỏ & Điều hướng | 8 | 53 | ~~2~~ → **0** | ✅ **XONG 2026-07-23** (2B, cả ba đợt A · B · C) | [modules/M14-NAVIGATION-SHELL/](modules/M14-NAVIGATION-SHELL/) |
| — Sa mạc (`camps`) | — | — | — | `NOT_STARTED` — chưa triển khai (Phase 8) | — |

---

## 2. Bảng 15 luồng CRITICAL

| ID | Module | Luồng | Điểm | Vấn đề cốt lõi | Ưu tiên |
|---|---|---|--:|---|---|
| ~~M01-F11~~ | Auth | Đăng xuất | 25 | ✅ **ĐÓNG ở M14-A** (A-01, D-86): nút Đăng xuất ở menu tài khoản + chân thanh bên + `/account`; M01-A xác nhận qua E2E | ✅ |
| ~~M01-F12~~ | Auth | Gán/đổi vai trò | 20 | ✅ **ĐÓNG ở M01-B** (TB-05): RPC `assign_primary_role` (nguyên tử, super-admin-only, trần vai trò) + nút "Đổi vai trò" ở `/staff/[id]`; đổi lớp giữ đăng nhập | ✅ |
| ~~M01-F03~~ | Auth | Tạo tài khoản | 42 | ✅ **ĐÓNG ở M01-B** (TB-01/D-102): `provisionAccountForStaff` tại `/staff/[id]` + trần vai trò (không tạo Super Admin thứ hai, không cấp vai trò ≥ mình) áp cả trên `/admin` | ✅ |
| M01-F08 | Auth | Xóa tài khoản | 48 | ✅ **ĐÓNG M01-C 2026-07-24** — hộp thoại gõ lại tên + nhật ký (M01-A) + **giữ lịch sử vai trò** (FK `role_assignments.profile_id` `cascade`→`set null`, D-101; pgTAP `028` JWT thật) | ~~P0~~ ✅ |
| ~~M04-F02~~ | Nhân sự | Tạo hồ sơ GLV | 43 | ✅ **ĐÓNG ở M04-B** (TB-M04-03): cảnh báo trùng SĐT / họ tên+ngày sinh **hai pha, mềm** — hiện danh sách nghi trùng, vẫn cho "Vẫn tạo hồ sơ mới"; nút tự khoá chặn bấm đúp; **không** thêm unique constraint (AC-05.2). Phản hồi + redirect `/staff/[id]` đã có từ M01-B | ✅ |
| ~~M04-F03~~ | Nhân sự | Sửa hồ sơ GLV | 29 | ✅ **ĐÓNG ở M01-B**: `updateStaff` (trước là action chết) kích hoạt qua khối "Sửa hồ sơ" ở `/staff/[id]` | ✅ |
| ~~M04-F06~~ | Nhân sự | Đổi lớp cho GLV | 24 | ✅ **ĐÓNG DỨT ĐIỂM ở M04-A** (D-105): một nút **"Chuyển lớp"** làm trọn trong MỘT giao dịch (`transfer_class_staff`), Trưởng/Phó ngành dùng được trong ngành mình. Trước đó M01-B mới chỉ cấp đủ **ba công cụ rời** (kết thúc → phân công → "Đổi vai trò") — vẫn còn cửa sổ để lại tài khoản zombie nếu dừng giữa chừng. AC-04.1 kiểm bằng JWT thật qua giao diện: GLV đăng nhập lại và điểm danh được lớp mới | ✅ |
| ~~M04-F05~~ | Nhân sự | Kết thúc phân công | 51 | ✅ **ĐÓNG ở M04-A** (C5 = 1 vì nút cũ giấu hệ quả): `ConfirmDialog` nêu tên người + tên lớp + **"và vô hiệu hoá vai trò đăng nhập"**, chỉ đường sang "Chuyển lớp"; ô ngày prefill + `min` theo `starts_on`. Nút trần ở `/staff` đã **gỡ** | ✅ |
| ~~M04-F04~~ | Nhân sự | Phân công vào lớp | 48 | ✅ **ĐÓNG ở M04-B**: phản hồi + câu lỗi nêu tên lớp/tên người (M04-A, AC-01.4); danh sách nay có **tìm kiếm không dấu + lọc lớp/trạng thái + phân trang** (TB-M04-04) | ✅ |
| ~~M04-F01~~ | Nhân sự | Danh sách nhân sự | 50 | ✅ **ĐÓNG ở M04-B** (TB-M04-04 + D-108): tìm theo tên/mã/SĐT (không dấu), lọc lớp + trạng thái phục vụ, phân trang 10/trang; trình độ huấn luyện **tiếng Việt** (hết `NONE`); mặc định ẩn "Đã nghỉ", **ghi rõ số người bị ẩn**; ba mức hiển thị tài khoản (D-110) | ✅ |
| ~~M04-F07~~ | Nhân sự | Xóa hồ sơ GLV | 51 | ✅ **ĐÓNG ở M04-B** (D-106/D-109): RPC `delete_unused_staff_profile` — chỉ xóa hồ sơ **chưa từng dùng** (không tài khoản, không phân công, 7 bảng tham chiếu = 0), gõ lại đúng họ tên (kiểm ở DB), `ConfirmDialog` nêu tên riêng, nhật ký D-65; quyền = 4 vai trò ghi toàn xứ đoàn; pgTAP `031` JWT thật (22 bài) | ✅ |
| ~~M04-F08~~ | Nhân sự | Đổi trạng thái phục vụ | 31 | ✅ **ĐÓNG ở M04-B** (5W-07): `service_status` hết bị hardcode `'active'` — hiện ở danh sách + trang chi tiết, đổi được ở khối "Sửa hồ sơ" (M01-B đã có ô); danh sách lọc theo trạng thái (D-108) | ✅ |
| M04-F09 | Nhân sự | Xem đội ngũ lớp | 66 `PASS` | ✅ **ĐIỂM TRỪ DUY NHẤT ĐÓNG ở M04-C**: audit ghi *"chưa link được sang hồ sơ GLV vì `/staff/[staffId]` chưa tồn tại"*. Trang đó có từ M01-B, nên đội ngũ lớp nay mở thẳng hồ sơ; người xem không đọc được hồ sơ (RLS) thì giữ chữ thường, **không** dựng liên kết dẫn tới trang trống. Nghiệp vụ giữ nguyên đúng như `PASS` yêu cầu | ✅ |
| ~~M02-F02~~ | Học vụ | Sinh 19 lớp | 44 | ✅ **ĐÓNG ở M02-A** (TB-F02, BR-M02-N01…N04): RPC trả `{inserted, expected, already_present}` thay vì một số 0 mang hai nghĩa; **ném lỗi** khi thiếu danh mục lớp chuẩn và khi năm học đã đóng; danh mục 5 ngành · 13 cấp · 19 mẫu lớp nay đi **theo migration** nên `supabase db push` lên project mới không còn để lại môi trường rỗng (5W-F11). Giao diện nói ba câu khác hẳn nhau: *vừa tạo* · *đã có đủ từ trước* · *thiếu danh mục*. pgTAP `032` 20 test JWT thật | ✅ |
| M02-F09 | Học vụ | Đóng năm học | 21 | **Chưa cài bước nào**; năm đã đóng vẫn ghi được | **P1** |
| ~~M03-F10~~ | Thiếu nhi | Kết thúc ghi danh | 35 | ✅ **ĐÓNG ở M03-A** (TB-F10): "Tạm nghỉ" tách hẳn thành thao tác riêng **không có ô ngày**, nên không còn đường nào vi phạm CHECK `enrollments_open_has_no_end` — chức năng này **chưa từng chạy được lần nào** kể từ Phase 2. Thêm **"Khôi phục"** (BR-M03-21, `docs/11` đòi từ đầu mà chưa từng có); "Kết thúc" nay có `ConfirmDialog` nêu **tên em + tên lớp** (C5 = 1 vì nút cũ ghi thẳng, không hỏi); huy hiệu trạng thái bằng **chữ** trên từng dòng; sĩ số **tách hai số** (D-121). Lý do "Chuyển lớp" giữ lại nhưng hộp thoại nói thẳng nó **không** ghi danh em vào lớp mới (D-122). pgTAP `036` 21 bài — trong đó ca `paused` kèm `ended_on` là **ca chưa từng được kiểm** | ✅ |
| ~~M03-F13~~ | Thiếu nhi | Cảnh báo trùng | 29 | ✅ **ĐÓNG ở M03-B** (TB-F13): luật "thế nào là trùng" chuyển lên `src/lib/students/duplicate.ts` dùng chung, nên đường gõ tay và đường Excel **không thể lệch mức cảnh báo** (AC-F13-04, có unit test canh đúng điều đó). Cảnh báo MỀM hai pha: thấy hồ sơ nghi trùng thì dừng lại nêu **tên + mã + ngày sinh + lớp**, người nhập bấm "Vẫn tạo hồ sơ mới" là qua (BR-M03-N08). Phép dò chạy dưới RLS của người thao tác qua khung nhìn `security_invoker` (AC-F13-03), không dùng service role. Người giám hộ cũng được dò theo tên + số điện thoại (BR-M03-N09) — đây là chuyện **phân quyền**, vì gia đình bị tách đôi làm phụ huynh chỉ thấy một phần số con. **Q-M03-06 = không lưu vết** (D-125) | ✅ |
| ~~M10-F03/F06~~ | Thông báo | Hộp thư & chuông | 38/47 | ✅ **ĐÓNG ở M10-A 2026-08-09** (TB-M10-01 / AC-01-01…07 / S-08 · S-09 · S-10). 🔴 **Hai lỗi là MỘT lỗi, và nó là một dòng VẮNG MẶT** — `getUnreadNotificationCount` (`queries.ts:42-49`) và truy vấn hộp thư (`:111-115`) đều thiếu `.eq("profile_id", …)`. Sáu hậu quả đo được: badge *"99+"* **không bao giờ về 0** · hộp thư cá nhân là 50 dòng của **toàn hệ thống** · nhãn "Mới" lấy `read_at` **của người khác** · một thông báo gửi 200 người hiện **200 lần** · nút "Đánh dấu đã đọc" **bấm mãi không tắt** (RPC chỉ đụng dòng của chính mình) · thông báo thật sự dành cho họ **bị đẩy khỏi 50 dòng đầu** ⇒ **bỏ lỡ thông báo**. Sửa ở **truy vấn**, **không** sửa policy — nhánh `or app.can_global_read()` vẫn cần cho màn hình quản trị (`07` §4). `unreadCount` lấy từ **phép đếm thật** thay vì đếm trên 50 dòng đã tải, và bọc `cache()` nên trang + chuông chỉ tốn **một** lượt đếm. **0 migration · 0 thay đổi phân quyền.** 🔴 **Lỗ hổng kiểm thử gốc rễ cũng đã đóng:** `03_AUDIT_RESULTS` §4.1 chỉ ra hai lỗi này sống sót vì **mọi** bài kiểm cũ đều chạy bằng **phiên phụ huynh** — vai **không** có quyền đọc toàn cục — nên chúng **không bao giờ chạm tới nhánh lỗi**. Nay có `tests/integration/m10-inbox-scope.test.ts` chạy bằng **JWT thật của Thư ký** và đo **cả hai chiều**: một bài **canh hiện trạng** ghim lại rằng cơ sở dữ liệu **cố ý** cho Thư ký đọc mọi dòng (nếu bài ấy đỏ nghĩa là ai đó đã sửa policy và bài kia trở nên vô nghĩa), một bài **bằng chứng dương tính** rằng truy vấn có `profile_id` trả về đúng và chỉ đúng thông báo của Thư ký — **6/6 xanh**. Cộng `notifications.spec.ts` **21/21 xanh cả ba viewport** và một **bài quét mã nguồn** canh cả hai bảng có policy chứa `or can_global_read()` | ✅ |
| ~~M12-F02/F07/F08~~ | Nhập Excel | Nhập & xóa lô | 40/45/38 | ✅ **ĐÓNG ở M12-A 2026-07-29** (TO-BE 1 · 2 · 3). **F02**: mặc định `create` cho **mọi** dòng — kể cả dòng trùng cả tên + ngày sinh + SĐT phụ huynh — nay là **Ghép**, và mức trùng chắc chắn **chặn ghi** cho tới khi người duyệt tự lưu quyết định (D-133, AC-18/19); dò trùng xét **mọi** hồ sơ, không chỉ hồ sơ `active` (AC-20), kèm cảnh báo riêng cho ca ghép vào em đã rút vì trigger của M03-C sẽ từ chối. **F07**: kết quả ghi hiện ra `Đã ghi X · bỏ qua Y · lỗi Z` kèm **từng dòng lỗi** đã dịch sang tiếng Việt (AC-15, AC-26/SEC-16 — trước đó in thẳng `sqlerrm`). **F08**: lần nhập **đã ghi** không xoá được kể cả gọi thẳng Data API (policy `import_batches_delete_dry_run`, pgTAP `039` **21 bài JWT thật**); lần nhập chưa ghi **huỷ = đánh dấu**, giữ nguyên hàng (D-131); "Xoá dữ liệu thô" tách riêng, giữ mapping `row → student` (D-132) | ✅ |
| ~~M12-F03/F04/F05~~ | Nhập Excel | Danh sách · chi tiết · giới tính | 58/50/42 | ✅ **ĐÓNG ở M12-B 2026-07-29** (TO-BE 4 · 7). **F05**: sổ SYLL thiếu giới tính ở **83% dòng** mà mỗi dòng là **một lần gửi + một lượt dựng lại cả trang** (tiêu chí "số bước hợp lý" chấm **2/5**, thấp nhất module) — nay cả trang đi lên trong **một** lượt gửi, kèm "Áp dụng Nam/Nữ cho các dòng đang chọn"; **không** có nút đoán theo tên đệm (BR-M12-36, `docs/09` §2b). **F04**: trang chi tiết hết đổ toàn bộ 300–900 dòng vào một lượt dựng — **50 dòng/trang + 6 bộ lọc trạng thái**, và ba con số ở đầu trang (dòng chờ ghi · dòng chưa xác nhận trùng · dòng thiếu giới tính) nay đếm **trong cơ sở dữ liệu** chứ không đếm trên trang đang xem. **F03**: danh sách lần nhập hết `.limit(20)` viết cứng — có phân trang, lọc theo năm học (**D-135**) và trạng thái, và **nói ai tải lên**. **D-133 vẫn đứng nguyên** (**D-136**): nút lưu chung không xác nhận hộ dòng trùng chắc chắn | ✅ |
| ~~M12-F01/F06~~ | Nhập Excel | Tải lên · tải kết quả | — | ✅ **ĐÓNG ở M12-C 2026-08-03** (TO-BE 5 · 6 · 8) ⇒ **M12 ĐÓNG**. **Tải file kết quả lần đầu tồn tại**: `docs/09` §9 đòi *"User download được errors"* từ Phase 2 mà chưa bao giờ có — nay `GET /imports/[batchId]/errors` xuất `.xlsx` hai sheet `LOI` + `KET_QUA` (kèm mapping `dòng → mã thiếu nhi`, lấy cả `matched_student_id` cho dòng đã ghép). Đây là **cầu nối duy nhất** tới Giáo lý viên lớp — người có dữ liệu còn thiếu nhưng không được vào `/imports` — mà **không nới quyền cho ai** (SEC-04b: route trả **403**, không phải file rỗng). Mọi ô chuỗi qua `safeSpreadsheetText` (AC-23/SEC-13, mở rộng thêm `TAB`/`CR`), và lỗi ghi được **dịch trước khi vào file** để SEC-16 không mở lại ở cửa thứ hai. **Ghi danh bị bỏ qua hết im lặng** (AC-24): `on conflict do nothing` làm dòng báo `committed` dù em vẫn ở lớp cũ — nay `commit_import_rows` trả `out_enrollment_created` (migration `20260803000100`, drop+create + **cấp lại grant**), dòng mang cảnh báo nêu **đúng tên lớp em đang học**, và lượt ghi đếm con số ấy **riêng**. **Hai cái trần** (**D-137** 4 MB — trần cũ 5 MB nằm **trên** giới hạn ~4,5 MB của nền tảng nên câu tiếng Việt chưa từng chạy được; **D-138** 1.000 dòng — **SEC-12 trước đây không có giới hạn nào**). **1 migration · 0 thay đổi phân quyền**, pgTAP `040` **20 bài JWT thật** (3 bài canh riêng phần `grant`) | ✅ |
| ~~M13-F01/F05/F08~~ | Cổng PH | Lối vào & quyền | 38/35/40 | ✅ **ĐÓNG ở M13-A 2026-08-12; toàn module đóng sau M13-B/C cùng ngày.** M14/M11 đã nối link; M13-A hoàn thiện D-64, D-25 và hàng rào route `/student`. Danh sách/chi tiết lọc `getMyChildren(profileId)`; E2E kiểm một con · nhiều con · GLV-phụ huynh · URL chéo 404/không lộ tên · vai trò sai bị chặn. M13-B/C trả trạng thái rỗng, khả năng đọc 360px, route và ba màn hình dùng chung. pgTAP **1385/1385**, portal E2E **24/24**; module không còn đợt A/B/C mở | ~~P0~~ |
| ~~M14-F05/F07~~ | Vỏ | Đổi năm học · Đăng xuất | 34/16 | ✅ **ĐÃ ĐÓNG 2026-07-23.** F05: năm học đọc từ `academic_years` thật (2B mục 0.9, D-84). F07: có `signOutAction` + nút Đăng xuất ở menu tài khoản và chân thanh bên (2B đợt M14-A, D-86) | ~~P0~~ |
| ~~M08-F01/F09~~ | Chuyển lớp | Bảng chuyển lớp · lọc theo ngành/lớp | 38/28 | ✅ **ĐÓNG ở M08-A 2026-08-06** (TO-BE 1 / AC-12 / AC-13). 🔴 **Lỗi này là một phép nhân:** `getPromotionsPageData` gọi `canProposeForClass` — **hai truy vấn** — cho **mỗi** ghi danh trong một `Promise.all`, và đọc **mọi năm học** không lọc không phân trang ⇒ với ~900 em là **~1.800 lượt đi về cơ sở dữ liệu cho một lượt mở trang**. Nay `getRepresentativeClassIds` hỏi **một lần cho cả trang** và toàn trang dùng **3–5 lượt gọi cố định** dù có 50 hay 5.000 dòng (AC-13 đòi ≤ 6). Kèm **bảng tiến độ theo lớp** lần đầu tồn tại — mỗi con số là liên kết vào đúng lớp ở đúng trạng thái, và phép đếm chạy **trên cả phạm vi chứ không trên trang đang xem** (bẫy M12-B đã vấp) — cộng lọc theo lớp/trạng thái, tìm tên **không dấu**, phân trang 25 dòng, và **BR-M08-14** thu về đúng năm học hiện hành (bản cũ đọc mọi năm — BR-M08-Y3). **0 migration · 0 thay đổi phân quyền.** `promotions.spec.ts` **48/48 xanh cả ba viewport** | ✅ |
| ~~M08-F03/F10~~ | Chuyển lớp | Sửa/gửi lại đề xuất · chuyển lớp giữa năm | 55/35 | ✅ **ĐÓNG ở M08-B 2026-08-07** (TO-BE 3 · 4 · 5 + D-159). **F03** — gửi lại một đề xuất bị từ chối **ghi đè sạch** ai từ chối, lúc nào, vì sao (`03_AUDIT_RESULTS` §4.3: *"yêu cầu idempotency được hiểu là yêu cầu ghi đè"*); nay có bảng **`promotion_review_events` chỉ-ghi-thêm** (**D-157**, chủ dự án chọn **ngược** khuyến nghị "cột `history` jsonb" của tài liệu, cùng lý do đã chọn `leaderboard_snapshots` ở M07-C). BR-M08-16 **giữ nguyên** — hàng review vẫn bị xoá `reviewed_*` khi gửi lại; cái đổi là lần từ chối ấy **đã nằm ngoài tầm với** của mệnh đề `do update` từ lúc nó xảy ra. Append-only chặn bằng **trigger**, nên luật đứng độc lập với mọi `grant`, kể cả `service_role`. **F10** — `/classes/[classId]` đóng được ghi danh đang có đề xuất `pending`, để lại một đề xuất **mồ côi** không bao giờ duyệt được và cũng không ai xoá được (BR-M08-Y2); nay chặn ở **cả hai tầng** (**D-158**, ngược khuyến nghị *"phương án A cho v1"*), phạm vi đúng **bốn trạng thái đóng** (**D-162** — "Tạm nghỉ" vẫn chạy vì nó không đóng ghi danh). Và *"chuyển lớp giữa năm"* lần đầu là một luồng **có chủ đích**: **D-159** cho bốn vai trò cấp xứ đoàn một RPC `promote_enrollment_now` làm cả hai bước **trong một giao dịch** — **0 thay đổi phân quyền**, họ vốn đã làm được việc này qua hai biểu mẫu. 🔴 **Quả mìn của đợt là THỨ TỰ LỆNH:** trigger F10 nằm trên `enrollments` và `security definer` **không** bỏ qua trigger, nên bản cũ (đóng ghi danh **rồi** mới đánh dấu review `approved`) sẽ tự chặn chính đường duyệt hợp lệ; lời giải là đảo thứ tự, không cần cờ phiên. Kèm **BR-M08-X1** lần đầu có consumer sau 5 phase (**D-156/D-161** — cảnh báo bí tích lớp cuối ngành, **không** hard-block, chỉ buộc nêu ý kiến) và **nợ #18** cho `promotion_reviews` (**D-160** — hỏi **cả hai** năm học). pgTAP `046` **50 bài JWT thật** | ✅ |
| ~~M09-F11~~ | Ban | Công việc tuần | — | ✅ **ĐÃ ĐÓNG 2026-07-23** (2B đợt M09-A). Form nạp sẵn bản của tuần đang chọn, nhãn nút đổi thành "Cập nhật công việc tuần", và `saveCommitteeWeeklyPlan` bỏ hẳn `upsert` — nay là INSERT khi chưa có bản, UPDATE **so sánh-và-đổi** trên `updated_at` khi đã có. Hai hàng rào kèm theo: CHECK `committee_weekly_plan_not_empty` chặn bản trắng ở DB, và `created_by` không còn bị ghi đè | ~~P1~~ |

---

## 3. Module PASS — giữ nguyên, không sửa nghiệp vụ

| Module | Vì sao PASS |
|---|---|
| **M06 — Giáo án** | 65/75. Phân quyền chặt, tài liệu để riêng bucket kín có đường dẫn hết hạn 60 giây, cổng phụ huynh chỉ thấy đúng phần được phép của tuần tới. Chỉ có góp ý giao diện nhỏ. |
| **M09 — Ban & Thiết bị** | 65/75. Mượn/trả thiết bị có khóa dòng, trả lại phiếu đã trả không cộng kho hai lần (75/75 tuyệt đối), người ngoài Ban mở đường dẫn trực tiếp không thấy gì. Một lỗi công việc tuần cần sửa — ✅ **đã đóng 2026-07-23** (2B đợt M09-A, F11). ⚠️ *2B đợt M09-B (2026-07-24) sửa thêm hai luồng `NEEDS_IMPROVEMENT` chứ không phải `PASS`: **F16** trả thiết bị nay tách "nhận lại hàng" khỏi "báo hỏng/mất" nên trả dần không còn ăn vào tổng kho (D-76), và **F19** có đường hợp lệ để đổi tổng kho (`adjust_equipment_stock`, D-92/D-98). Ba luồng trong danh sách "không đụng" của `06` §2 vẫn nguyên: khoá dòng khi mượn, trả lại phiếu đã đóng là idempotent, người ngoài Ban không thấy gì.* ✅ **M09 ĐÓNG 2026-07-24 (2B đợt M09-C).** Giao diện: trang chi tiết chuyển sang tabs, sửa Ban/lịch họp (dùng policy đang bỏ không), ma sát tương xứng cho thao tác phá huỷ (ConfirmDialog + chức vụ controlled), `ends_on` do DB đặt. **D-100** đóng nợ tồn từ Phase 6: thành viên cùng Ban đọc được đầy đủ hồ sơ nhau (trước đó hiện `—`), kiểm bằng JWT thật. E2E `committees.spec.ts` 9/9 xanh 3 viewport.* |
| **Chuỗi M05→M07→M11/M13** | Chuỗi dữ liệu chính của hệ thống. Xem `03_CROSS_MODULE_WORKFLOWS.md` CM-03. |

**31 luồng PASS riêng lẻ** nằm rải rác ở mọi module — chi tiết trong `03_AUDIT_RESULTS.md` của từng module.

---

## 4. Kết luận tầng hạ tầng

| Hạng mục | Trạng thái | Tài liệu |
|---|---|---|
| Kiến trúc phân quyền 3 lớp | `PASS` | [02_ROLE_PERMISSION_MAP.md](02_ROLE_PERMISSION_MAP.md) §1, §8 |
| Hàm quyền cơ sở dữ liệu | `PASS` | §3 |
| Ghi dữ liệu nhạy cảm bắt buộc qua hàm chuyên dụng | `PASS` | §4 |
| Bất biến của báo cáo đã chốt | `PASS` | §4 |
| Quyền quản trị cao nhất chỉ dùng phía máy chủ | `PASS` | §6.6 |
| Quyền ghi hồ sơ theo ngành | `NEEDS_CONFIRMATION` | §6.1 |
| Quyền chốt báo cáo của Cha sở/Cha phó | `NEEDS_CONFIRMATION` | §6.3 |
| Phân biệt "rỗng" với "không có quyền" | `NEEDS_IMPROVEMENT` | §6.4, §6.5 |

---

## 5. Điều hướng tài liệu

| Tài liệu | Nội dung |
|---|---|
| [01_SYSTEM_MODULE_MAP.md](01_SYSTEM_MODULE_MAP.md) | Bản đồ 14 module, nền tảng kỹ thuật, đồ thị phụ thuộc |
| [02_ROLE_PERMISSION_MAP.md](02_ROLE_PERMISSION_MAP.md) | Ma trận 14 vai trò × quyền, trích từ mã nguồn thật |
| [03_CROSS_MODULE_WORKFLOWS.md](03_CROSS_MODULE_WORKFLOWS.md) | 10 quy trình đi qua nhiều module |
| [04_SYSTEM_WIDE_FINDINGS.md](04_SYSTEM_WIDE_FINDINGS.md) | 14 vấn đề lặp lại + **16 điểm mạnh không được phá** |
| [05_REDESIGN_PRIORITY_PLAN.md](05_REDESIGN_PRIORITY_PLAN.md) | Kế hoạch 6 đợt (đã cập nhật theo quyết định đã chốt) |
| 📌 [**06_DECISION_LOG.md**](06_DECISION_LOG.md) | **19 quyết định D-61…D-79 chốt 2026-07-23 — nguồn sự thật, ghi đè mọi mô tả cũ** |
| `modules/<ID>/01..08` | Chi tiết từng module |

---

## 6. Trạng thái audit

| Module | Trạng thái audit | Ngày |
|---|---|---|
| M01, M02, M03, M04, M05, M06, M07, M08, M09, M10, M11, M12, M13, M14 | ✅ Đã audit đủ 8 tài liệu | 2026-07-23 |
| Sa mạc (`camps`) | ⏸ Chưa triển khai — ngoài phạm vi Giai đoạn 1 (Phase 8) | — |

**Giai đoạn 1 kết thúc tại đây.** Không sửa mã nguồn, không chạy migration, không đổi cơ sở dữ liệu,
không redesign giao diện.

## 7. ✅ Sẵn sàng vào Giai đoạn 2

**19 quyết định đã được chốt ngày 2026-07-23** → [`06_DECISION_LOG.md`](06_DECISION_LOG.md).
Không còn gì chặn.

**Việc đầu tiên của Giai đoạn 2:** Đợt 1 — 10 hạng mục sửa nhanh, 3–5 ngày, không hạng mục nào
cần đổi cơ sở dữ liệu. Xem [`05_REDESIGN_PRIORITY_PLAN.md`](05_REDESIGN_PRIORITY_PLAN.md).

**Ba điều người triển khai phải biết trước:**
1. **Đọc `06_DECISION_LOG.md` trước tiên** — nó ghi đè `docs/03`, `docs/05`, `docs/06` và `WORKLOG.md`.
2. **Có nhật ký thao tác** (D-65) — đảo ngược quyết định cũ D-34; là hạng mục lớn ở Đợt 2.
3. **Không đụng 16 điểm mạnh** ở [`04_SYSTEM_WIDE_FINDINGS.md`](04_SYSTEM_WIDE_FINDINGS.md).

---

## 8. Kết quả kiểm định độc lập Giai đoạn 3 — 2026-08-13

> Phần này là phụ lục kiểm định mới nhất và **ghi đè trạng thái sẵn sàng phát hành**, nhưng không
> xóa lịch sử audit/triển khai ở các phần trên. Nhãn “đã đóng 14/14 module” của Giai đoạn 2B chỉ
> xác nhận các đợt triển khai đã được thực hiện; nó không tự động chứng minh toàn bộ quyết định,
> acceptance criterion, bảo mật, toàn vẹn dữ liệu và hành trình browser đã đạt.

### 8.1 Kết luận điều hành

**NO-GO — chưa thể xác nhận Giai đoạn 2 hoàn tất và chưa sẵn sàng triển khai production.**

Lý do chặn chính:

1. **D-65 chưa được triển khai đúng phạm vi toàn hệ thống:** thiếu nhật ký chung cho khoảng 30 thao
   tác/12 nhóm, before/after + địa chỉ truy cập, cơ chế redaction, và màn hình chỉ Super Admin đọc.
2. **M02 có hai đường chuyển niên khóa không đồng nhất:** đường đặt năm hiện hành có thể đóng năm
   cũ mà bỏ qua checklist, metadata và quy trình chuyển tiếp đã duyệt.
3. **M07 chưa bảo vệ cập nhật đồng thời cùng một ô điểm:** stale save vẫn có thể last-write-wins,
   trái AC-03-01 nếu AC này chưa được chủ dự án chính thức thay thế.
4. **M11 chưa có báo cáo live theo năm lịch sử; M06 còn cửa sổ lệch trạng thái Storage/DB.**
5. **Full E2E cuối còn đỏ:** 571/585 pass, 14 fail; không có lỗi `ECONNREFUSED`. Cả 14 artifact
   đều mang chữ ký độ tin cậy UI: Server Action/RSC/feedback, derived state hoặc navigation bị kẹt
   dưới tải tuần tự. Không artifact nào tự chứng minh mutation nghiệp vụ ghi sai dữ liệu.

### 8.2 Quality gates trên baseline cuối

| Gate | Kết quả |
|---|---|
| Lint | **PASS** — 0 error, 0 warning |
| TypeScript | **PASS** |
| Build production | **PASS** — 29 page/route |
| Unit/default test | **PASS** — 1.545 pass, 18 skip; 111 file pass, 5 file skip |
| Reset DB + toàn bộ migration | **PASS** — tới `20260813000400` |
| pgTAP | **PASS** — 55 file, 1.442 assertion |
| Integration/concurrency/import/scope/perf | **PASS** trên baseline local đã ghi trong báo cáo regression |
| Full E2E, 23 spec × 3 viewport | **FAILED** — 571/585 pass, 14 fail, 32,2 phút |

### 8.3 Trạng thái 14 module sau verification

| Module | Trạng thái Giai đoạn 3 | Điểm chặn chính |
|---|---|---|
| M01 Auth & Accounts | **FAILED** | D-65 thiếu phạm vi/redaction/viewer |
| M02 Academic Years & Classes | **FAILED · NEEDS_CONFIRMATION** | Hai workflow chuyển/đóng niên khóa không đồng nhất |
| M03 Students & Guardians | **FAILED** | D-65 và E2E phản hồi vòng đời chưa ổn định |
| M04 Staff | **FAILED** | D-65; bằng chứng browser cuối chưa xanh |
| M05 Attendance | **FAILED** | D-65 cho mở khóa/sửa sau mở khóa chưa có |
| M06 Teaching Plans | **FAILED** | Storage/DB không nguyên tử, thiếu retry/outbox |
| M07 Results | **FAILED · NEEDS_CONFIRMATION** | Same-cell stale save chưa conflict; E2E còn đỏ |
| M08 Promotions | **FAILED** | D-65 và doc drift chưa được hòa giải |
| M09 Committees & Equipment | **FAILED · NEEDS_CONFIRMATION** | Audit toàn hệ thống chưa đủ; drift quyết định/AC |
| M10 Notifications | **FAILED** | D-65 cho publish/retract chưa đủ |
| M11 Reports & Dashboard | **FAILED** | Không có live report năm lịch sử; D-65 |
| M12 Imports | **FAILED** | Hardening đã tốt hơn nhưng D-65 và E2E cuối còn đỏ |
| M13 Portal | **BLOCKED** | Ownership/RLS có nền tảng, nhưng browser gate chưa đủ để VERIFIED |
| M14 Navigation & Shell | **VERIFIED_WITH_MINOR_ISSUES** *(có điều kiện)* | Còn phụ thuộc full browser/a11y manual và doc drift |

Tổng hợp: **12 FAILED · 1 BLOCKED · 1 VERIFIED_WITH_MINOR_ISSUES có điều kiện · 0 VERIFIED tuyệt đối.**

### 8.4 Hồ sơ bằng chứng

Đọc bộ báo cáo chính thức tại [`verification/`](verification/):

1. [`01_VERIFICATION_PLAN.md`](verification/01_VERIFICATION_PLAN.md)
2. [`02_BUSINESS_FLOW_VERIFICATION.md`](verification/02_BUSINESS_FLOW_VERIFICATION.md)
3. [`03_PERMISSION_SECURITY_REVIEW.md`](verification/03_PERMISSION_SECURITY_REVIEW.md)
4. [`04_DATA_INTEGRITY_REVIEW.md`](verification/04_DATA_INTEGRITY_REVIEW.md)
5. [`05_UI_UX_REVIEW.md`](verification/05_UI_UX_REVIEW.md)
6. [`06_E2E_TEST_MATRIX.md`](verification/06_E2E_TEST_MATRIX.md)
7. [`07_REGRESSION_RESULTS.md`](verification/07_REGRESSION_RESULTS.md)
8. [`08_OPEN_ISSUES.md`](verification/08_OPEN_ISSUES.md)
9. [`09_FINAL_SYSTEM_ASSESSMENT.md`](verification/09_FINAL_SYSTEM_ASSESSMENT.md)

Artifact của lượt full cuối nằm tại
`verification/evidence/full-e2e-20260813-final/`; SHA-256 của `.last-run.json` là
`75EC8062FAA20960951A094C1FB190F4C10EDFB60DCECEED67C27646F5274659`. HTML report trong thư mục
này là artifact cũ không khớp và đã được ghi rõ trong `RUN_SUMMARY.md`; không dùng nó để chứng minh
kết quả final. Danh tính 14 failure dựa trên `.last-run.json` và 14 `error-context.md`.
